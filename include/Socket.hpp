#ifndef SOCKET_HPP
#define SOCKET_HPP

#include "mainHeader.hpp"

class Socket
{
	public:
	Socket();
	Socket(const Socket &other);
	Socket operator=(const Socket &other);
	~Socket();

	private:
	
};

#endif